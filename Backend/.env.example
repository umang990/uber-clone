# Server port
PORT=3000

# MongoDB connection string
DB_CONNECT=mongodb://localhost:27017/uber-clone

# JWT secret key for authentication (use a strong random string)
JWT_SECRET=

# Google Maps API key (enable Geocoding, Distance Matrix, and Places APIs)
GOOGLE_MAPS_API=
